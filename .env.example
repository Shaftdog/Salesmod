# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Public Supabase URL (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Public anon key (safe to expose to client - used for client-side auth)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (KEEP SECRET - only use on server, never expose to client)
# This bypasses Row Level Security - handle with extreme care!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Application URL
# Used for email redirects and magic links
# Development: http://localhost:9002
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:9002

# Optional: Database connection for direct queries (if needed)
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project-ref.supabase.co:5432/postgres

# Optional: OpenAI API Key (for AI features if you add them later)
# OPENAI_API_KEY=sk-...

# Optional: Tavily API Key (for web search in chat agent)
# Get your free API key at: https://tavily.com
# TAVILY_API_KEY=tvly-...

# Optional: Sentry DSN for error tracking
# SENTRY_DSN=https://...

# Optional: Analytics
# NEXT_PUBLIC_GA_ID=G-...

# ============================================================================
# SETUP INSTRUCTIONS:
# ============================================================================
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Get your Supabase credentials:
#    - Go to: https://supabase.com/dashboard
#    - Select your project
#    - Go to: Settings > API
#    - Copy the values into .env.local
#
# 3. Update NEXT_PUBLIC_APP_URL:
#    - Development: http://localhost:9002
#    - Production: Your actual domain
#
# 4. NEVER commit .env.local to git!
#    (It's already in .gitignore)
#
# ============================================================================
# SECURITY NOTES:
# ============================================================================
#
# - NEXT_PUBLIC_* variables are exposed to the browser
# - SUPABASE_SERVICE_ROLE_KEY must NEVER be exposed to the client
# - Always use the executeAsAdmin() wrapper for service-role operations
# - Row Level Security (RLS) protects your data at the database level
#
